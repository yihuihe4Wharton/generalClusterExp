% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{.calc_treatment_probability}
\alias{.calc_treatment_probability}
\title{Within-cluster / cluster-level assignment probability}
\usage{
.calc_treatment_probability(
  dist,
  params,
  treated_neighbors,
  total_neighbors_in_cluster,
  total_size,
  cluster_id = NULL
)
}
\arguments{
\item{dist}{\code{"binom"} or \code{"hypergeom"}.}

\item{params}{for \code{"binom"}, \code{list(prob = p)}; for
\code{"hypergeom"}, \code{list(m = m)} where \code{m} may be a scalar or a
per-cluster vector.}

\item{treated_neighbors}{number of treated draws (vectorised).}

\item{total_neighbors_in_cluster}{number of draws.}

\item{total_size}{population size of the cluster (or the number of clusters
for a cluster-level law).}

\item{cluster_id}{optional cluster id used to pick the entry of a
per-cluster \code{params$m}.}
}
\value{
A numeric vector of probabilities.
}
\description{
Probability of \code{treated_neighbors} treated out of
\code{total_neighbors_in_cluster} draws, under the within-cluster (or
cluster-level) assignment law \code{dist} with parameters \code{params}.
}
\examples{
## Binomial law: P(2 of 3 neighbours treated) with p = 0.5
generalClusterExp:::.calc_treatment_probability(
  "binom", list(prob = 0.5), 2, 3, total_size = 10)

## Hypergeometric law: 2 treated among 3 draws from a cluster of
## size 10 containing m = 5 treated units
generalClusterExp:::.calc_treatment_probability(
  "hypergeom", list(m = 5), 2, 3, total_size = 10)
}
\keyword{internal}
