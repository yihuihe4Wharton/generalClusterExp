% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance.R
\name{.run_engine}
\alias{.run_engine}
\title{Estimation engine: weights to tidy output}
\usage{
.run_engine(
  A,
  C,
  Y,
  W_C,
  W_Y,
  weights1,
  weights0,
  p1_own,
  p0_own,
  unit_weights,
  method,
  design
)
}
\arguments{
\item{A}{n_unit x n_unit adjacency matrix (with self-loops).}

\item{C}{integer vector of cluster ids.}

\item{Y}{numeric vector of observed outcomes.}

\item{W_C}{0/1 vector of cluster-level treatments.}

\item{W_Y}{0/1 vector of individual-level treatments.}

\item{weights1, weights0}{per-regime identification weights (from one of the
weight constructors).}

\item{p1_own, p0_own}{per-unit own-treatment probabilities under phi_1 and
phi_0.}

\item{unit_weights}{optional per-unit weights (default: uniform).}

\item{method}{\code{"mrn"}, \code{"iptw"} or \code{"crn"}.}

\item{design}{\code{"bernoulli"} or \code{"complete"}.}
}
\value{
See \code{\link{mrn}}.
}
\description{
Main engine shared by \code{\link{mrn}}, \code{\link{iptw}} and
\code{\link{crn}}: takes the per-regime identification weights, enumerates
the well-defined potential outcomes, forms the Hajek LW point estimates and
contribution columns, assembles the HAC covariance, and returns the tidy
output lists.
}
\examples{
set.seed(1)
A <- Matrix::Matrix(outer(1:12, 1:12, function(i, j) abs(i - j) <= 1) * 1,
                    sparse = TRUE)
C <- rep(1:3, each = 4); W_C <- c(1, 0, 1)
W_Y <- rbinom(12, 1, ifelse(W_C[C] == 1, 0.5, 0.2))
Y <- rnorm(12, mean = 2 * as.vector(A \%*\% W_Y))
w <- generalClusterExp:::.iptw_weights(A, C, W_C,
                                       distC = "binom",
                                       paramsC = list(prob = 0.7))
out <- generalClusterExp:::.run_engine(A, C, Y, W_C, W_Y,
  weights1 = w$weights1, weights0 = w$weights0,
  p1_own = rep(0.5, 12), p0_own = rep(0.2, 12),
  unit_weights = NULL, method = "iptw", design = "bernoulli")
out$causal_effects
}
\keyword{internal}
