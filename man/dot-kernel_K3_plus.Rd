% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernels.R
\name{.kernel_K3_plus}
\alias{.kernel_K3_plus}
\title{Unit-level interference kernel K3+}
\usage{
.kernel_K3_plus(A, psd_fun = "shift")
}
\arguments{
\item{A}{n_unit x n_unit adjacency matrix (with self-loops).}

\item{psd_fun}{PSD projection: \code{"shift"} (default, sparse) or
\code{"eigen"} (dense, exact PSD part).}
}
\value{
An n_unit x n_unit PSD kernel matrix.
}
\description{
PSD part of the unit-level interference kernel
\deqn{(K_3)_{ij,i'j'} = 1\{ N_{ij} \cap N_{i'j'} \neq \emptyset \},}
i.e. two units are dependent when they share at least one interference
neighbour. The PSD part \eqn{K_3^+} is the kernel of the cluster-agnostic
variance estimator \eqn{\hat\Sigma_2} (Theorem 5.9).
}
\examples{
## chain network: each unit interferes with itself and adjacent units
A <- Matrix::Matrix(outer(1:6, 1:6, function(i, j) abs(i - j) <= 1) * 1,
                    sparse = TRUE)
K3 <- generalClusterExp:::.kernel_K3_plus(A)
round(as.matrix(K3), 2)  # overlap indicator (plus a small diagonal shift)
}
\keyword{internal}
