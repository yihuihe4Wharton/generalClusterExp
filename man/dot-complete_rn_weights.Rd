% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{.complete_rn_weights}
\alias{.complete_rn_weights}
\title{Complete / joint Radon--Nikodym (CRN) identification weights}
\usage{
.complete_rn_weights(
  A,
  C,
  W_C,
  W_Y,
  dist1 = "binom",
  params1 = list(prob = 0.5),
  dist0 = "binom",
  params0 = list(prob = 0),
  unit_weights = NULL
)
}
\arguments{
\item{A}{n_unit x n_unit adjacency matrix (with self-loops).}

\item{C}{integer vector of cluster ids.}

\item{W_C}{0/1 vector of cluster-level treatments, indexed by cluster id.}

\item{W_Y}{0/1 vector of individual-level treatments.}

\item{dist1, params1}{within-cluster assignment law of the treated regime phi_1.}

\item{dist0, params0}{within-cluster assignment law of the control regime phi_0.}

\item{unit_weights}{optional per-unit weights (default: uniform); only units
with nonzero weight are processed.}
}
\value{
A list with numeric vectors \code{weights1} and \code{weights0}.
}
\description{
Per-unit complete (joint) Radon--Nikodym weights \eqn{\alpha_{ij}^{comp}}
(Prop. weights (ii)); cluster-agnostic. Ported from
\code{our_helper::Radon_weights}.
}
\examples{
set.seed(1)
A <- Matrix::Matrix(outer(1:12, 1:12, function(i, j) abs(i - j) <= 1) * 1,
                    sparse = TRUE)
C <- rep(1:3, each = 4)
W_C <- c(1, 0, 1)
W_Y <- rbinom(12, 1, ifelse(W_C[C] == 1, 0.5, 0.2))
w <- generalClusterExp:::.complete_rn_weights(A, C, W_C, W_Y,
  dist1 = "binom", params1 = list(prob = 0.5),
  dist0 = "binom", params0 = list(prob = 0.2))
cbind(phi1 = w$weights1, phi0 = w$weights0)
}
\keyword{internal}
